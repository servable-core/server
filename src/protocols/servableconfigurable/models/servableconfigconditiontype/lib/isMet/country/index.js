
export default async props => {
    const {
        user,
        installation,
        useMasterKey,
        ip,
        headers,
        options,
        condition,
        conditionValue,
        conditionType } = props
    return user
}