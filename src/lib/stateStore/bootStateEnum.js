// Stored as plain numbers. Same values the old keynum SeedStateEnum/ConfigStateEnum had, so
// records imported from the legacy utils database keep their meaning.
export default Object.freeze({
  Initial: 0,
  Loading: 1,
  LoadedSuccessfully: 2,
  ErrorLoading: 3,
})
